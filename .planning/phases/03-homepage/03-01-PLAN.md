---
phase: 03-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/components/ScrollReveal.tsx
autonomous: true

must_haves:
  truths:
    - "The hero section loads with a full-bleed background image, dark overlay, headline, subheadline, dual CTA buttons, and a lead capture form card on the right"
    - "The proof/stats bar (25+ Years, 312 Reviews, 4.9 Stars, A+ BBB) appears inside the hero section adjacent to the CTA, not as a separate section"
    - "The hero background image is loaded via next/image with priority prop — no layout shift on load"
    - "A wave SVG divider at the bottom of the hero transitions smoothly to the white services section below"
    - "The page metadata title renders as 'Plumber in Omaha, NE | Heartland Plumbing Co.' without duplication"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Homepage with hero section, metadata, and schema injection"
      contains: "export default function HomePage"
    - path: "src/components/ScrollReveal.tsx"
      provides: "Lightweight IntersectionObserver scroll animation wrapper"
      contains: "use client"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/lib/data/business.ts"
      via: "import BUSINESS"
      pattern: "import.*BUSINESS.*from.*business"
    - from: "src/app/page.tsx"
      to: "src/lib/schema/local-business.ts"
      via: "import generateLocalBusinessSchema"
      pattern: "generateLocalBusinessSchema"
    - from: "src/app/page.tsx"
      to: "src/components/JsonLd.tsx"
      via: "import JsonLd"
      pattern: "JsonLd.*data="
---

<objective>
Build the homepage hero section by porting the existing HTML prototype design to Next.js server components. This creates the above-the-fold experience: dark background with plumber photo, headline + subheadline, dual CTA buttons, a lead capture form card, trust signals row, and proof stats bar — all inside the hero. Also creates the ScrollReveal utility component and sets up page metadata + schema injection scaffolding that Plan 02 will build upon.

Purpose: The hero is the first thing visitors see. It must communicate trust, show the phone number, and offer a form — all above the fold. This is the primary conversion surface.
Output: A working hero section at `/` with correct LCP image, metadata, and schema.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage/03-CONTEXT.md
@.planning/phases/03-homepage/03-RESEARCH.md

@src/app/layout.tsx
@src/lib/data/business.ts
@src/lib/data/services.ts
@src/lib/data/testimonials.ts
@src/lib/data/service-areas.ts
@src/components/JsonLd.tsx
@src/lib/schema/local-business.ts
@src/lib/schema/aggregate-rating.ts
@src/lib/metadata.ts
@src/app/globals.css
@src/components/layout/Header.tsx
@C:\Users\andre\OneDrive - Creighton University\Desktop\Claude-Code-Projects\website-creation\google-business-profile-htmls\heartland-plumbing-omaha.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ScrollReveal client component</name>
  <files>src/components/ScrollReveal.tsx</files>
  <action>
Create a lightweight `'use client'` component that wraps children in a div and uses IntersectionObserver to add a `.visible` class on scroll-into-view. Requirements:

1. Accept `children` and optional `className` props
2. Use `useRef` + `useEffect` with IntersectionObserver (threshold: 0.1)
3. On intersection, add `visible` class and disconnect observer
4. Default styles: `opacity-0 translate-y-8 transition-all duration-700`
5. Visible styles: `[&.visible]:opacity-100 [&.visible]:translate-y-0`
6. Respect `prefers-reduced-motion`: check `window.matchMedia('(prefers-reduced-motion: reduce)').matches` — if true, set visible immediately without animation
7. Keep this under 25 lines — it's a tiny client island

This is the ONLY new client component for the homepage. All section content remains server-rendered.
  </action>
  <verify>File exists, contains `'use client'`, contains `IntersectionObserver`, contains `prefers-reduced-motion` check. Run `npx tsc --noEmit` to verify no type errors.</verify>
  <done>ScrollReveal.tsx exists as a working client component that animates children on scroll with reduced-motion support.</done>
</task>

<task type="auto">
  <name>Task 2: Build homepage hero section with metadata and schema</name>
  <files>src/app/page.tsx</files>
  <action>
Replace the placeholder `page.tsx` with the full homepage. In THIS plan, only build the hero section — leave placeholder comments for remaining sections that Plan 02 will fill in.

**Metadata (top of file):**
- Export `metadata` with `title: 'Plumber in Omaha, NE'` (the layout template appends `| Heartland Plumbing Co.`)
- Description: `'Licensed Omaha plumber since 1998. 4.9-star rating, 312 reviews. Same-day service, 24/7 emergency calls. Free estimates — call (402) 555-0147.'` (keep under 155 chars)
- `alternates: { canonical: '/' }`
- Do NOT duplicate OG/twitter tags — they inherit from root layout metadataBase

**Schema injection:**
- Import and call `generateLocalBusinessSchema()` and `generateAggregateRatingSchema()`
- Render both via `<JsonLd data={...} />` at the top of the return

**Hero section structure (port from HTML prototype `heartland-plumbing-omaha.html`):**

1. `<section>` with `relative min-h-[95vh] overflow-hidden flex items-center` and dark background
2. Background image: `next/image` with `fill`, `priority`, `sizes="100vw"`, src `https://images.unsplash.com/photo-1607472586893-edb57bdc0e39?w=1920&q=80`, empty alt (decorative), `aria-hidden="true"`
3. Dark gradient overlay div (gradient from left to right, rgba values from prototype)
4. SVG wave divider at bottom — fill color must be `#ffffff` (white, matching the Services section below per RESEARCH.md Pitfall 4)
5. Content container: `max-w-[1320px] mx-auto px-9 w-full relative z-[4]`
6. Grid layout: `grid-cols-1 lg:grid-cols-[1.15fr_1fr] gap-12 items-center`

**Left column (headline + CTA + proof):**
- Badge/pill: "Omaha's Trusted Plumbing Experts" (small teal badge like prototype)
- H1: "Expert Plumbing Services in Omaha, NE" (use `font-display` / Bitter, large white text)
- Subheadline: 1-2 sentences about same-day service, 25+ years, licensed (use `text-white/80`, Nunito Sans)
- Dual CTA buttons:
  - Primary: `<a href={BUSINESS.phoneHref}>` "Call (402) 555-0147" — teal/primary background, large, with phone icon
  - Secondary: `<Link href="/contact">` "Get Free Estimate" — outline/bordered style
- Trust signals row: Google rating (4.9 stars with star icons), "Licensed & Insured" badge, "BBB A+ Rated" — use `text-white/70` with small icons
- Proof stats bar (INSIDE hero, below trust row): 4-column grid with `25+` Years, `312` Reviews, `4.9` Stars, `A+` BBB. Numbers in `font-display font-black text-[2.2rem] text-primary-light`, labels in `text-white/70 text-sm`. Border-top separator `border-white/10`. On mobile: `grid-cols-2`.

**Right column (lead form card):**
- White card with shadow (`shadow-hero-form` token from globals.css if it exists, otherwise `shadow-2xl`)
- Card header: "Request a Free Estimate" in `font-display`
- Form fields: First Name, Last Name, Phone (required, primary), Email (optional), Service dropdown (populated from `services` data slugs/titles), Message textarea
- Submit button: copper/accent background, full width, "Get Your Free Estimate"
- Trust micro-copy below form: "Your info is safe" | "Response in < 1 hour" | "100% Free" — small text with lock/clock/check icons
- Form `action="/contact"` (static, form handling built in Phase 6)
- Add a `{/* TODO: Wire form submission in Phase 6 */}` comment

**After the hero section, add placeholder comments:**
```tsx
{/* Services Grid Section — Plan 03-02 */}
{/* Trust Signals Section — Plan 03-02 */}
{/* Testimonials Section — Plan 03-02 */}
{/* Service Areas Section — Plan 03-02 */}
```

**Import all data sources** at the top even if not all used yet in this plan — Plan 02 will use them:
- `BUSINESS` from `@/lib/data/business`
- `services` from `@/lib/data/services`
- `testimonials` from `@/lib/data/testimonials`
- `areas` from `@/lib/data/service-areas`

**Styling guidelines:**
- Use existing Tailwind token classes: `bg-dark`, `bg-primary`, `bg-copper`, `text-primary-light`, `font-display`, `font-body`, `border-border`
- Reference Header.tsx and Footer.tsx for confirmed working class names
- Mobile-first: hero grid stacks to single column below `lg`, proof bar goes to 2 cols below `lg`, form card goes full width
- No `'use client'` on this file — it is a Server Component
  </action>
  <verify>
1. `npm run build` succeeds with no errors
2. `npm run dev` — visit `http://localhost:3000` — hero section visible with background image, headline, form, and proof stats
3. View page source — confirm JSON-LD script tags for LocalBusiness and AggregateRating are present
4. Check `<title>` tag renders as "Plumber in Omaha, NE | Heartland Plumbing Co."
5. No `'use client'` directive in page.tsx
  </verify>
  <done>Homepage hero section renders with dark background image (priority loaded), headline, dual CTAs, lead form card, trust signals row, proof stats bar inside hero, wave divider, and correct metadata + schema injection. Page builds without errors.</done>
</task>

</tasks>

<verification>
- `npm run build` completes with 0 errors
- Homepage route `/` renders the hero section with all elements visible
- LCP image has `priority` prop (check Next.js build output for preloaded images)
- JSON-LD contains valid LocalBusiness and AggregateRating schemas
- Title tag is "Plumber in Omaha, NE | Heartland Plumbing Co." (no duplication)
- No client JS beyond ScrollReveal.tsx and existing MobileNav
- Hero is responsive: single column on mobile, two columns on desktop
</verification>

<success_criteria>
- Hero section matches the visual weight and layout of the HTML prototype
- Proof stats bar is inside the hero, adjacent to CTA (locked decision)
- Background image loads with priority (no lazy loading)
- Form card is present with all fields and trust micro-copy
- Wave divider transitions cleanly to white background below
- Page metadata and schema are correct
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-homepage/03-01-SUMMARY.md`
</output>
