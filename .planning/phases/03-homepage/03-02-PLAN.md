---
phase: 03-homepage
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/page.tsx
autonomous: true

must_haves:
  truths:
    - "A services grid section displays all 4 services with icons, descriptions, and links to /services/[slug]"
    - "A trust signals section shows 4 icon+stat cards (25+ Years, 312 Reviews, 4.9 Stars, Licensed PL-28541) on an off-white background with a CTA button"
    - "A testimonials section displays 3 customer cards with gold star ratings, quote text, name, and city on a white background"
    - "A service areas section on a dark background lists all 8 cities as links to /service-areas/[slug]"
    - "All internal links work: service cards link to /services/*, area links go to /service-areas/*"
    - "Section backgrounds alternate: white (services) -> off-white (trust) -> white (testimonials) -> dark (areas)"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Complete homepage with all 5 sections"
      contains: "ServicesGrid"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/lib/data/services.ts"
      via: "import services, map to Link components"
      pattern: "services\\.map"
    - from: "src/app/page.tsx"
      to: "src/lib/data/testimonials.ts"
      via: "import testimonials, slice first 3"
      pattern: "testimonials.*slice\\(0.*3\\)|testimonials\\[0\\]"
    - from: "src/app/page.tsx"
      to: "src/lib/data/service-areas.ts"
      via: "import areas, map to Link components"
      pattern: "areas\\.map"
    - from: "src/app/page.tsx (service links)"
      to: "/services/[slug]"
      via: "Next.js Link component"
      pattern: "href=.*services/"
    - from: "src/app/page.tsx (area links)"
      to: "/service-areas/[slug]"
      via: "Next.js Link component"
      pattern: "href=.*service-areas/"
---

<objective>
Build the remaining four homepage sections below the hero: Services Grid, Trust Signals, Testimonials, and Service Areas. Replace the placeholder comments left by Plan 01 with fully implemented sections. Each section uses data from the existing TypeScript data files and links to the corresponding detail pages.

Purpose: These sections provide the content depth, social proof, and internal linking that drive both SEO value and visitor conversion below the fold.
Output: A complete, fully-rendered homepage with all 5 sections, correct background alternation, and all internal links wired.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-homepage/03-CONTEXT.md
@.planning/phases/03-homepage/03-RESEARCH.md
@.planning/phases/03-homepage/03-01-SUMMARY.md

@src/app/page.tsx
@src/lib/data/services.ts
@src/lib/data/testimonials.ts
@src/lib/data/service-areas.ts
@src/lib/data/business.ts
@src/components/ScrollReveal.tsx
@src/components/layout/Header.tsx
@src/components/layout/Footer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Services Grid and Trust Signals sections</name>
  <files>src/app/page.tsx</files>
  <action>
Replace the `{/* Services Grid Section — Plan 03-02 */}` and `{/* Trust Signals Section — Plan 03-02 */}` placeholder comments in page.tsx with fully implemented sections.

**Services Grid Section (white background `bg-white`):**
- Container: `py-20` padding, `max-w-[1320px] mx-auto px-9`
- Section header: H2 "Our Plumbing Services" in `font-display`, centered, with a short subtitle in `text-text-secondary`
- Grid: `grid-cols-1 sm:grid-cols-2 gap-6` (2x2 on desktop)
- Each card maps over `services` array and renders:
  - An SVG icon appropriate to the service (water drop for drain cleaning, thermometer for water heaters, pipe/lines for sewer, clock for emergency — use inline SVGs, 40x40, stroke-based with `text-primary` color)
  - `service.title` as H3 in `font-display font-bold`
  - `service.shortDescription` truncated to first sentence or ~120 chars, in `text-text-secondary text-sm`
  - "Learn More" link as `<Link href={/services/${service.slug}}>` with arrow icon, `text-primary font-semibold`
- Cards: white background with `border border-border` and `hover:shadow-card` transition, `p-7` padding
- Wrap the entire section in `<ScrollReveal>`

**Trust Signals Section (off-white background `bg-off-white`):**
- Container: `py-20`, `max-w-[1320px] mx-auto px-9`
- Section header: H2 "Why Omaha Homeowners Trust Us" centered
- Grid: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6`
- 4 cards, each with:
  1. Shield icon + "25+" large number + "Years in Business" label
  2. Star icon + "312" + "Google Reviews"
  3. Star-half icon + "4.9" + "Star Rating"
  4. Badge/certificate icon + "PL-28541" + "Licensed & Insured"
- Card style: white background, centered content, `p-6`, icon in `text-primary` at 48x48, number in `font-display font-black text-3xl text-dark`, label in `text-text-secondary text-sm`
- Use BUSINESS constants for the numbers (BUSINESS.rating.count, BUSINESS.rating.value, BUSINESS.license) — do NOT hardcode
- CTA below the cards: centered, `mt-10`
  - `<a href={BUSINESS.phoneHref}>` "Call for a Free Estimate" — `bg-primary text-white font-bold py-4 px-8 text-lg` with hover state
- Wrap in `<ScrollReveal>`
  </action>
  <verify>
1. `npm run build` succeeds
2. Visit homepage — services grid shows 4 cards with icons, titles, descriptions, and "Learn More" links
3. Trust signals section shows 4 stat cards with correct numbers from BUSINESS constant
4. Service card links point to `/services/drain-cleaning`, `/services/water-heaters`, etc.
5. Background alternation visible: white (services) -> off-white (trust)
  </verify>
  <done>Services grid displays all 4 services with working links to detail pages. Trust signals section shows 4 stat cards with BUSINESS data and a CTA button. Both sections have correct backgrounds and scroll reveal animation.</done>
</task>

<task type="auto">
  <name>Task 2: Build Testimonials and Service Areas sections</name>
  <files>src/app/page.tsx</files>
  <action>
Replace the `{/* Testimonials Section — Plan 03-02 */}` and `{/* Service Areas Section — Plan 03-02 */}` placeholder comments with fully implemented sections.

**Testimonials Section (white background `bg-white`):**
- Container: `py-20`, `max-w-[1320px] mx-auto px-9`
- Section header: H2 "What Our Customers Say" centered, `font-display`
- Grid: `grid-cols-1 md:grid-cols-3 gap-6`
- Select exactly 3 testimonials: use research recommendation — Mike R. (Omaha, emergency), Sarah T. (Papillion, drain/sewer), Linda P. (La Vista, water heater). Find Linda P. in the testimonials array — if not available by exact name, pick the third testimonial that covers water heaters or a different service type than the first two.
- Each testimonial card:
  - `bg-off-white border border-border p-7` (matches prototype style)
  - Gold star rating: render `testimonial.rating` filled stars using the star SVG polygon from RESEARCH.md. Stars in `text-amber-500` (gold).
  - Blockquote: `testimonial.text` in `text-text-secondary text-sm leading-relaxed italic`
  - Author line: `testimonial.name` bold + `testimonial.city` in `text-text-muted text-sm` separated by comma
- Wrap in `<ScrollReveal>`

**Service Areas Section (dark background `bg-dark`):**
- Container: `py-20`, `max-w-[1320px] mx-auto px-9`
- Section header: H2 "Serving the Greater Omaha Area" in `text-white font-display` centered, with subtitle in `text-white/70`
- Grid: `grid-cols-2 sm:grid-cols-4 gap-4`
- Map over `areas` array (imported from service-areas.ts). Each cell:
  - `<Link href={/service-areas/${area.slug}}>` wrapping the content
  - Map pin SVG icon (small, inline, `text-primary-light`) + area name in `text-white font-semibold`
  - Border: `border border-white/10` with `hover:border-primary/50` transition
  - Padding: `p-4`, centered text
- Below the grid, add a CTA: "Need a Plumber? Call Now" linking to `BUSINESS.phoneHref`, styled as `bg-copper text-white font-bold py-3 px-8` centered, `mt-10`
- Wrap in `<ScrollReveal>`

**Final cleanup:**
- Remove any remaining placeholder comments from Plan 01
- Ensure all imports at the top are actually used (no unused imports)
- Verify the section order in JSX matches: Hero -> Services -> Trust -> Testimonials -> Areas
- Run `npm run build` to confirm zero errors
  </action>
  <verify>
1. `npm run build` succeeds with 0 errors
2. Visit homepage — all 5 sections render in correct order with correct backgrounds
3. Testimonials show 3 cards with gold stars, quote text, and author info
4. Service areas show 8 city links in a grid on dark background
5. Click a service area link — navigates to `/service-areas/[slug]`
6. Click a testimonial star — no broken interactivity (stars are decorative)
7. No placeholder comments remain in page.tsx
8. View page source — JSON-LD still present from Plan 01
  </verify>
  <done>Homepage is complete with all 5 sections: Hero, Services Grid, Trust Signals, Testimonials, Service Areas. All internal links work. Background alternation follows the locked pattern. Three testimonials display with gold stars. Eight area links render on dark background. Page builds successfully with schema injection intact.</done>
</task>

</tasks>

<verification>
- `npm run build` completes with 0 errors
- Homepage renders all 5 sections in correct order with correct background alternation
- Services grid: 4 cards, each linking to `/services/[slug]`
- Trust signals: 4 stat cards using BUSINESS data, CTA button with phone link
- Testimonials: 3 cards with gold stars, quotes, names, cities
- Service areas: 8 city links to `/service-areas/[slug]` on dark background
- All data is imported from data files (no hardcoded strings for business info)
- ScrollReveal wraps each below-fold section
- No unused imports, no placeholder comments remain
- JSON-LD schemas still present in page source
</verification>

<success_criteria>
- Complete homepage with 5 visually distinct sections
- Background pattern: dark -> white -> off-white -> white -> dark
- All 4 service links work
- All 8 area links work
- Trust signals show correct BUSINESS data (not hardcoded)
- 3 testimonials with diverse service coverage
- CTA buttons present in trust signals and service areas sections
- Page builds and renders without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-homepage/03-02-SUMMARY.md`
</output>
