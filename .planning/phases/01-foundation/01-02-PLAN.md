---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/layout/Header.tsx
  - src/components/layout/MobileNav.tsx
  - src/components/layout/Footer.tsx
  - src/components/layout/MobileCTA.tsx
  - src/app/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Header is sticky at top of viewport, shows logo + nav + phone CTA on desktop"
    - "Header gains box-shadow when user scrolls past the top"
    - "Phone number in header is a tappable tel: link with 48x48px minimum tap target"
    - "Mobile hamburger menu opens and closes smoothly, showing all nav links"
    - "Services dropdown in navigation shows all 4 service links"
    - "Sticky Call Now bar is visible at bottom of screen on mobile and hidden on desktop"
    - "Footer displays consistently across all pages with service areas, hours, phone, address"
    - "No content is hidden behind the sticky mobile CTA bar"
  artifacts:
    - path: "src/components/layout/Header.tsx"
      provides: "Server Component sticky header with logo, desktop nav, phone CTA"
      contains: "sticky top-0"
    - path: "src/components/layout/MobileNav.tsx"
      provides: "Client Component hamburger menu with toggle state"
      contains: "use client"
    - path: "src/components/layout/Footer.tsx"
      provides: "Server Component footer with service areas, hours, contact info"
      contains: "footer"
    - path: "src/components/layout/MobileCTA.tsx"
      provides: "Fixed bottom Call Now bar, hidden on md+"
      contains: "fixed bottom-0"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/layout/Header.tsx"
      via: "import and render in root layout"
      pattern: "import.*Header"
    - from: "src/components/layout/Header.tsx"
      to: "src/components/layout/MobileNav.tsx"
      via: "Server Component imports Client Component island"
      pattern: "import.*MobileNav"
    - from: "src/app/layout.tsx"
      to: "src/components/layout/MobileCTA.tsx"
      via: "import and render in root layout"
      pattern: "import.*MobileCTA"
---

<objective>
Build the complete layout shell: sticky Header (Server Component) with desktop nav and phone CTA, MobileNav (Client Component) with hamburger toggle, Footer, and mobile sticky CTA bar. Wire all into root layout.tsx.

Purpose: Every page on the site shares this layout. Getting header, footer, and mobile CTA right in Phase 1 means no layout rework in later phases.
Output: Four layout components wired into root layout, responsive across all breakpoints.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Header, MobileNav, and scroll shadow effect</name>
  <files>
    src/components/layout/Header.tsx
    src/components/layout/MobileNav.tsx
  </files>
  <action>
    **Header.tsx (Server Component — NO 'use client'):**

    1. Sticky header: `sticky top-0 z-50 bg-white border-b border-border transition-shadow duration-300`
    2. Container: `max-w-[1320px] mx-auto px-[36px]` (matches hero HTML container)
    3. Inner flex: `flex justify-between items-center py-[14px] gap-6`
    4. Logo section:
       - Teal rounded square (44x44px, bg-primary, rounded-md) with inline SVG pipe icon (copy from hero HTML)
       - "Heartland Plumbing Co." text in font-display (Bitter), font-bold
       - Wrap in Link to "/"
    5. Desktop nav (hidden below lg, `hidden lg:flex items-center gap-[28px]`):
       - Links: Services (with dropdown), Areas, About, Contact
       - Services dropdown: on hover, show a dropdown panel listing all 4 services (Drain Cleaning, Water Heaters, Sewer Line Repair, Emergency Plumbing) with links to /services/[slug]
       - Use CSS hover for dropdown (group/hover pattern), no JS needed
       - Nav link styling: `text-text-secondary hover:text-primary font-semibold text-[15px]`
    6. Header right section (hidden below lg, `hidden lg:flex items-center gap-[16px]`):
       - Phone number: (402) 555-0147 as small text
       - CTA button: "Call Now" with phone icon, `bg-copper hover:bg-copper-dark text-white px-[22px] py-[10px] rounded-sm font-bold text-sm`
       - Both phone text and CTA button are `<a href="tel:4025550147">` links
       - Ensure minimum 48x48px tap target on the CTA button
    7. Add `id="site-header"` to the `<header>` element for scroll shadow targeting
    8. Import and render `<MobileNav />` inside the header (visible only below lg)

    **MobileNav.tsx ('use client'):**

    1. Hamburger button: visible only below lg (`lg:hidden`), three-bar icon, toggles `open` state
    2. Mobile menu: full-width overlay or slide-in panel below header
       - Shows all nav links: Services (expanded list of 4 services), Areas, About, Contact
       - Phone number with tel: link
       - "Call Now" CTA button
       - Close on link click or close button
    3. Use `useState` for open/close toggle
    4. Smooth transition: use CSS transition on max-height or transform for slide

    **Scroll shadow effect:**
    Add a `useEffect` inside MobileNav.tsx (since it's already a client component) OR create a tiny separate HeaderScrollWatcher client component:
    - Listen to `window.scroll` event
    - When `window.scrollY > 0`, add class `shadow-header` to `#site-header`
    - When scrollY is 0, remove it
    - Use `document.getElementById('site-header')` to target the server-rendered header
    - This keeps Header.tsx as a Server Component while adding the scroll effect

    **Important layout values from hero HTML:**
    - Container: max-width 1320px, padding 0 36px
    - Header padding: 14px top/bottom
    - Logo icon: 44x44px
    - Nav gap: 28px
    - Header-right gap: 16px
    - CTA padding: 10px 22px
    - Breakpoint: lg (1024px) — hide desktop nav, show hamburger below this
  </action>
  <verify>
    - `npm run build` passes (no type errors, no import errors)
    - Desktop (>1024px): Logo + nav links + phone + CTA button visible; hamburger hidden
    - Mobile (<1024px): Logo + hamburger visible; nav links and phone hidden; hamburger click opens menu
    - Scroll down: header gains shadow; scroll to top: shadow removed
    - Phone CTA is a working tel: link
    - Services dropdown on desktop shows 4 service links on hover
  </verify>
  <done>Header renders correctly at all breakpoints. Desktop shows full nav with services dropdown and phone CTA. Mobile shows hamburger that toggles a menu. Scroll shadow appears on scroll.</done>
</task>

<task type="auto">
  <name>Task 2: Build Footer, MobileCTA, and wire all into root layout</name>
  <files>
    src/components/layout/Footer.tsx
    src/components/layout/MobileCTA.tsx
    src/app/layout.tsx
  </files>
  <action>
    **Footer.tsx (Server Component — NO 'use client'):**

    1. Dark background: `bg-dark text-white`
    2. Container: `max-w-[1320px] mx-auto px-[36px]`
    3. Padding: `py-[60px] pb-6` on desktop, add `pb-20` on mobile (md:pb-6) to clear sticky CTA bar
    4. Footer grid (responsive):
       - Column 1: Company info — logo (small version), brief tagline, phone number as tel: link, address (Omaha, NE)
       - Column 2: Services — links to all 4 service pages (/services/drain-cleaning, etc.)
       - Column 3: Service Areas — links to all 8 area pages (/service-areas/omaha, etc.)
       - Column 4: Hours — Monday-Friday 7AM-6PM, Saturday 8AM-2PM, Sunday Closed; "24/7 Emergency Service" highlighted
    5. Bottom bar: copyright line "2026 Heartland Plumbing Co. All rights reserved." + "Licensed & Insured — PL-28541"
    6. Grid layout: 4 columns on lg, 2 columns on sm, 1 column on mobile

    **MobileCTA.tsx (Server Component — it's just a link, no state needed):**

    Per CONTEXT.md decisions:
    1. Fixed position: `fixed bottom-0 left-0 right-0 z-[9999]`
    2. Background: `bg-primary` (teal #1a7a6e)
    3. Hidden on desktop: `md:hidden`
    4. Shadow: `shadow-mobile-cta`
    5. Content: Single "Call Now" button with phone SVG icon
       - `<a href="tel:4025550147">` wrapping the entire bar content
       - Text: "Call Now — (402) 555-0147"
       - Styling: `text-white font-bold text-sm flex items-center justify-center gap-2 w-full py-3 rounded-md`
    6. Padding wrapper: `px-4 py-3`

    **Update src/app/layout.tsx:**

    1. Import Header, Footer, and MobileCTA
    2. Render in this order inside <body>:
       ```
       <Header />
       <main>{children}</main>
       <Footer />
       <MobileCTA />
       ```
    3. Ensure <body> still has `pb-20 md:pb-0` to prevent content hiding behind sticky CTA
    4. Keep all existing font config and metadata from Plan 01

    **Remove the test page content from src/app/page.tsx:**
    Replace with a minimal placeholder that shows the layout works:
    - A centered section with "Homepage — Coming in Phase 3" text
    - This confirms the header/footer/CTA are rendering around page content
  </action>
  <verify>
    - `npm run build` passes
    - Desktop: Header at top, footer at bottom, no sticky CTA bar visible
    - Mobile: Header at top, footer at bottom, teal "Call Now" bar fixed at bottom
    - Footer content is NOT hidden behind the sticky CTA bar on mobile
    - All footer links point to correct routes (/services/*, /service-areas/*)
    - Phone number in MobileCTA is a working tel: link
    - git commit and push — Vercel deploy shows the layout shell
  </verify>
  <done>Complete layout shell renders: sticky header with nav, footer with service/area links, and mobile CTA bar. All wired into root layout. Deployed to Vercel.</done>
</task>

</tasks>

<verification>
1. `npm run build` exits with code 0
2. Desktop viewport (>1024px): sticky header with logo + nav + phone CTA; footer with 4-column grid; no mobile CTA bar
3. Mobile viewport (<1024px): sticky header with hamburger; footer visible above sticky CTA; teal "Call Now" bar fixed at bottom
4. Hamburger menu opens/closes with smooth transition
5. Services dropdown on desktop shows 4 service links
6. All tel: links work
7. Scroll shadow appears on header when scrolling down
8. Footer content not obscured by mobile CTA bar
</verification>

<success_criteria>
- Header is sticky, responsive (hamburger on mobile, full nav on desktop), with working phone CTA
- Services dropdown shows all 4 services
- Footer displays service links, area links, hours, contact info
- Mobile CTA bar is always visible on mobile, hidden on desktop
- No layout content is hidden behind sticky elements
- Layout deployed to Vercel successfully
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
