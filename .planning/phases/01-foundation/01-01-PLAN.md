---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "npm run build completes with zero errors"
    - "npm run dev serves a page at localhost:3000 with Bitter and Nunito Sans fonts visible"
    - "Tailwind v4 @theme tokens generate working utility classes (bg-primary renders teal)"
    - "Project is pushed to GitHub and Vercel deploy URL returns a working page"
  artifacts:
    - path: "next.config.ts"
      provides: "Next.js config with remotePatterns and image formats"
      contains: "remotePatterns"
    - path: "postcss.config.mjs"
      provides: "Tailwind v4 PostCSS plugin"
      contains: "@tailwindcss/postcss"
    - path: "src/app/globals.css"
      provides: "Brand tokens via @theme directive"
      contains: "@theme"
    - path: "src/app/layout.tsx"
      provides: "Root layout with next/font CSS variables on html element"
      contains: "font-bitter"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "import './globals.css'"
      pattern: "import.*globals"
    - from: "postcss.config.mjs"
      to: "src/app/globals.css"
      via: "@tailwindcss/postcss processes @theme tokens"
      pattern: "@tailwindcss/postcss"
---

<objective>
Initialize the Heartland Plumbing Next.js 15 project with TypeScript, Tailwind CSS v4, Sharp, and all required dependencies. Configure next.config.ts, postcss for Tailwind v4, brand tokens in globals.css, and root layout with next/font. Push to GitHub and connect to Vercel.

Purpose: Create the project skeleton that every subsequent phase builds on. Fonts, colors, and build pipeline must work before any components are written.
Output: A working Next.js 15 project deployed to Vercel with Tailwind v4 brand tokens and self-hosted fonts.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 project and upgrade to Tailwind v4</name>
  <files>
    package.json
    tsconfig.json
    next.config.ts
    postcss.config.mjs
    .gitignore
  </files>
  <action>
    Run from the project root (heartland-plumbing/):

    1. Scaffold the project INTO the current directory (it already has CLAUDE.md and .planning/):
       ```
       npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"
       ```
       If prompted about existing files, allow overwrite of defaults but preserve CLAUDE.md and .planning/.
       If create-next-app refuses to run in a non-empty directory, scaffold into a temp folder and move files:
       ```
       npx create-next-app@latest heartland-temp --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"
       ```
       Then move all files from heartland-temp/ into the current directory, preserving existing .planning/ and CLAUDE.md. Remove the temp folder.

    2. Upgrade to Tailwind v4:
       ```
       npm install tailwindcss@latest @tailwindcss/postcss@latest postcss@latest
       ```

    3. Install Sharp for local dev image optimization:
       ```
       npm install sharp
       ```

    4. Update postcss.config.mjs to use the v4 plugin:
       ```js
       export default { plugins: { '@tailwindcss/postcss': {} } }
       ```

    5. Delete tailwind.config.ts (or .js) if create-next-app generated one — v4 uses @theme in CSS only.

    6. Update next.config.ts per research:
       ```typescript
       import type { NextConfig } from 'next'
       const config: NextConfig = {
         images: {
           remotePatterns: [
             { protocol: 'https', hostname: 'images.unsplash.com' },
           ],
           formats: ['image/avif', 'image/webp'],
         },
       }
       export default config
       ```

    7. Verify .gitignore includes node_modules, .next, .env*.local.
  </action>
  <verify>
    Run `npm run dev` — dev server starts without errors.
    Run `npm run build` — build completes with zero errors.
  </verify>
  <done>Next.js 15 project exists with Tailwind v4, Sharp, correct PostCSS config, and no tailwind.config.js file.</done>
</task>

<task type="auto">
  <name>Task 2: Configure brand tokens, root layout with next/font, and deploy</name>
  <files>
    src/app/globals.css
    src/app/layout.tsx
    src/app/page.tsx
  </files>
  <action>
    1. Replace src/app/globals.css with the complete brand token config from RESEARCH.md:
       - `@import "tailwindcss";`
       - `@theme { ... }` block with ALL 16 color tokens, font tokens (--font-display, --font-body), border radius (--radius-sm/md/lg), and shadows (--shadow-card, --shadow-hero-form, --shadow-header, --shadow-mobile-cta)
       - Remove any v3 directives (@tailwind base/components/utilities)
       - Exact values in RESEARCH.md "Complete globals.css Structure" section

    2. Update src/app/layout.tsx per RESEARCH.md:
       - Import Bitter and Nunito_Sans from next/font/google
       - Bitter: subsets ['latin'], weight ['400', '700', '900'], variable '--font-bitter', display 'swap'
       - Nunito_Sans: subsets ['latin'], weight ['400', '600', '700'], variable '--font-nunito', display 'swap'
       - Apply both variables to <html> className
       - Set <body> className to "font-body text-text-primary antialiased pb-20 md:pb-0"
       - Add metadata export with metadataBase, title template, and description per RESEARCH.md

    3. Update src/app/page.tsx to a simple test page that confirms tokens work:
       - H1 in font-display (Bitter) with text "Heartland Plumbing Co."
       - H2 in font-body (Nunito Sans) with text "Omaha's Trusted Plumber"
       - A div with bg-primary and text-white showing "Primary Teal"
       - A div with bg-copper and text-white showing "Copper Accent"
       - A tel: link styled as the phone CTA
       - This is a temporary page — it will be replaced in Phase 3

    4. Create GitHub repo and push:
       ```
       git init (if not already)
       git add -A
       git commit -m "feat: scaffold Next.js 15 project with Tailwind v4 brand tokens"
       git remote add origin https://github.com/Andrew-J-Wegner/heartland-plumbing.git
       git push -u origin main
       ```
       Note: If the repo doesn't exist yet on GitHub, create it:
       ```
       gh repo create heartland-plumbing --public --source=. --remote=origin --push
       ```

    5. Connect to Vercel (if vercel CLI available):
       ```
       npx vercel --yes
       ```
       If vercel CLI is not available or auth fails, provide manual steps for Andrew to connect the repo in the Vercel dashboard.
  </action>
  <verify>
    - `npm run build` passes with zero errors
    - `npm run dev` shows the test page with Bitter font on H1, Nunito Sans on body text, teal bg-primary div, copper bg-copper div
    - GitHub repo exists with the initial commit
    - Vercel deploy URL returns the test page (or manual steps provided for Andrew)
  </verify>
  <done>
    Brand tokens render correctly (bg-primary is teal #1a7a6e, bg-copper is #b8733a), fonts are self-hosted via next/font (no external font requests in DevTools Network tab), project is on GitHub, and Vercel deploy shows a working page.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` exits with code 0
2. `npm run dev` serves localhost:3000 with visible Bitter + Nunito Sans fonts
3. Browser DevTools Network tab shows NO requests to fonts.googleapis.com
4. `bg-primary` renders as teal (#1a7a6e) on the test page
5. No tailwind.config.js or tailwind.config.ts file exists in the project root
6. postcss.config.mjs references `@tailwindcss/postcss`
7. GitHub repo has the initial commit
</verification>

<success_criteria>
- Next.js 15 project builds and deploys successfully
- Tailwind v4 @theme tokens generate correct utility classes
- Bitter and Nunito Sans load via next/font with zero render-blocking requests
- Project is live on Vercel (or manual deploy steps documented)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
