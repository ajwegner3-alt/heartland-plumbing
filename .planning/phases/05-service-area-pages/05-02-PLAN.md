---
phase: 05-service-area-pages
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Each area page H1 matches format 'Plumber in [City], NE'"
    - "No two area pages share any title tag or meta description"
    - "Each area page links to all 4 service pages (/services/drain-cleaning, /services/water-heaters, /services/sewer-line-repair, /services/emergency-plumbing)"
    - "Each area page links to exactly 7 other area pages (not itself)"
    - "Breadcrumb on each area page shows Home > Service Areas > Plumber in [City], NE"
    - "/service-areas index lists all 8 areas with working links"
    - "JSON-LD on each area page contains areaServed with city name"
  artifacts: []
  key_links:
    - from: "src/app/service-areas/[slug]/page.tsx"
      to: "/services/*"
      via: "4-card service grid links"
      pattern: "href=.*services/"
    - from: "src/app/service-areas/[slug]/page.tsx"
      to: "/service-areas/*"
      via: "We Also Serve pill links"
      pattern: "href=.*service-areas/"
---

<objective>
Verify all 8 service area pages, the index page, schema markup, cross-links, and content uniqueness.

Purpose: Confirm AREA-01 through AREA-10 requirements are satisfied and no build/type errors exist. This is a verification-only plan -- if 05-01 pre-built everything correctly, this plan confirms it and produces the summary.

Output: Verification results documented in 05-02-SUMMARY.md. No file changes expected unless issues found.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-service-area-pages/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify build, routes, and type safety</name>
  <files></files>
  <action>
1. Run `npx tsc --noEmit` and confirm zero type errors
2. Run `npm run build` and confirm:
   - Build succeeds with exit code 0
   - All 9 routes appear in output: /service-areas (index), plus all 8 area slugs
   - Specifically verify /service-areas/la-vista appears (hyphenated slug test)
3. Check the generated sitemap includes `/service-areas` as a standalone URL
  </action>
  <verify>`npx tsc --noEmit` exits 0. `npm run build` exits 0 and lists all 9 service-area routes.</verify>
  <done>Build passes, all routes generated, no type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Verify content uniqueness, cross-links, and schema</name>
  <files></files>
  <action>
1. **Content uniqueness check:** Read `src/app/service-areas/[slug]/page.tsx` and confirm:
   - H1 uses `area.city` (produces unique H1 per page)
   - Title comes from `area.metaTitle` (already unique in service-areas.ts)
   - Description comes from `area.metaDescription` (already unique)
   - localContext[] paragraphs are rendered (3 per area, all unique in data file)

2. **Cross-links check:** Confirm the page template includes:
   - Links to all 4 service pages via the services grid (imports `services` array)
   - Links to other 7 areas via "We Also Serve" section (filters current area out)
   - Breadcrumb links to `/` and `/service-areas`

3. **Schema check:** Confirm JSON-LD includes:
   - `generateServiceAreaSchema(area)` call producing Plumber type with `areaServed: { '@type': 'City', name: area.city }`
   - `generateBreadcrumbSchema()` with 3-level path

4. **Index page check:** Read `src/app/service-areas/page.tsx` and confirm:
   - Lists all 8 areas with links to `/service-areas/${area.slug}`
   - Has BreadcrumbList JSON-LD (2-level)
   - Has static metadata with unique title/description

5. If any issues found, fix them. If everything passes, document in summary.
  </action>
  <verify>
    All checks pass: unique content per area, 4 service links + 7 area links on each page, valid schema, working index.
  </verify>
  <done>
    All AREA-01 through AREA-10 requirements verified. Each area has unique local content, links to all services, links to other areas, and valid schema markup.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. 9 service-area routes in build output
3. No shared H1, title, or meta description between any two area pages
4. Each area page has 4 service links + 7 area cross-links
5. JSON-LD contains city-scoped areaServed on each area page
6. /service-areas index lists all 8 areas
</verification>

<success_criteria>
- Build passes with all routes
- Content uniqueness confirmed (no duplicate titles, descriptions, or H1s)
- Cross-links verified (services + areas)
- Schema markup valid
- Requirements AREA-01 through AREA-10 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-service-area-pages/05-02-SUMMARY.md`
</output>
