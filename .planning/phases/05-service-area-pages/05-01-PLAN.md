---
phase: 05-service-area-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/schema/service-area.ts
  - src/app/service-areas/[slug]/page.tsx
  - src/app/service-areas/page.tsx
  - src/app/sitemap.ts
autonomous: true

must_haves:
  truths:
    - "Navigating to /service-areas/omaha returns a pre-rendered page with Omaha-specific content"
    - "Navigating to /service-areas/la-vista returns a pre-rendered page (hyphenated slug works)"
    - "All 8 area pages have unique H1, title tag, and meta description"
    - "Each area page links to all 4 service pages"
    - "Each area page links to the other 7 area pages"
    - "/service-areas index page lists all 8 areas with links"
    - "npm run build succeeds with all 8 area routes + index generated"
  artifacts:
    - path: "src/lib/schema/service-area.ts"
      provides: "City-scoped Plumber LocalBusiness schema generator"
      exports: ["generateServiceAreaSchema"]
    - path: "src/app/service-areas/[slug]/page.tsx"
      provides: "Dynamic area page with generateStaticParams + generateMetadata"
      min_lines: 100
    - path: "src/app/service-areas/page.tsx"
      provides: "Service areas index page listing all 8 cities"
      min_lines: 30
  key_links:
    - from: "src/app/service-areas/[slug]/page.tsx"
      to: "src/lib/data/service-areas.ts"
      via: "areas.find(a => a.slug === slug)"
      pattern: "areas\\.find"
    - from: "src/app/service-areas/[slug]/page.tsx"
      to: "src/lib/schema/service-area.ts"
      via: "generateServiceAreaSchema(area)"
      pattern: "generateServiceAreaSchema"
    - from: "src/app/service-areas/[slug]/page.tsx"
      to: "/services/[slug]"
      via: "Link href to service pages"
      pattern: "href=.*services/"
    - from: "src/app/service-areas/[slug]/page.tsx"
      to: "/service-areas/[other-slug]"
      via: "We Also Serve cross-links"
      pattern: "href=.*service-areas/"
---

<objective>
Build all 8 service area pages, the service areas index page, and the city-scoped schema generator.

Purpose: Closes the pre-existing sitemap 404s for all /service-areas/* URLs and fulfills requirements AREA-01 through AREA-10 (unique local content per city, cross-links to services and other areas).

Output: 3 new files + 1 updated file. All 8 area routes pre-render at build time. /service-areas index exists for breadcrumb resolution.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-service-area-pages/05-CONTEXT.md
@.planning/phases/05-service-area-pages/05-RESEARCH.md
@src/lib/data/service-areas.ts
@src/app/services/[slug]/page.tsx
@src/app/services/page.tsx
@src/lib/schema/local-business.ts
@src/lib/schema/breadcrumb.ts
@src/app/sitemap.ts
@src/lib/metadata.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create schema generator and dynamic area page</name>
  <files>
    src/lib/schema/service-area.ts
    src/app/service-areas/[slug]/page.tsx
  </files>
  <action>
**1. Create `src/lib/schema/service-area.ts`:**

Export `generateServiceAreaSchema(area: AreaData)` returning `WithContext<LocalBusiness>`.

- Use `@type: 'Plumber'` (same as existing local-business.ts)
- Set `url` to `${BUSINESS.url}/service-areas/${area.slug}`
- Set `areaServed` to `{ '@type': 'City', name: area.city, containedInPlace: { '@type': 'State', name: 'Nebraska' } }`
- Include `aggregateRating` with BUSINESS.rating values (same as local-business.ts)
- Include `address` and `telephone` from BUSINESS
- Cast with `as unknown as WithContext<LocalBusiness>` (same pattern as local-business.ts — required for schema-dts strict typing)

**2. Create `src/app/service-areas/[slug]/page.tsx`:**

Mirror the service page structure but simpler (no FAQ, no emergency variant, no pricing sidebar).

**Static generation:**
- `generateStaticParams()` returning `areas.map(a => ({ slug: a.slug }))`
- `generateMetadata()` using `generatePageMetadata({ title: area.metaTitle, description: area.metaDescription, path: /service-areas/${area.slug} })`
- Use `async` params pattern: `{ params }: { params: Promise<{ slug: string }> }` then `const { slug } = await params`

**Page sections (in this order):**

A. **JSON-LD schemas** (rendered before visible content, same as service pages):
   - `generateServiceAreaSchema(area)` via `<JsonLd>`
   - `generateBreadcrumbSchema([Home, Service Areas, "Plumber in {city}, NE"])` via `<JsonLd>`
   - Breadcrumb items: `{ name: 'Home', url: BUSINESS.url }`, `{ name: 'Service Areas', url: '${BUSINESS.url}/service-areas' }`, `{ name: 'Plumber in ${area.city}, NE', url: '${BUSINESS.url}/service-areas/${area.slug}' }`

B. **Banner section** (`bg-dark py-16 lg:py-20`):
   - Visual breadcrumb nav: Home / Service Areas / {city} (links to /, /service-areas)
   - H1: `Plumber in {area.city}, NE` (matches CONTEXT.md decision)
   - Subtext: First sentence of `area.localContext[0]` truncated, or a brief area-serving tagline
   - Dual CTA: Call button + Get Free Estimate link (same pattern as services)
   - Trust micro-bar: License, rating, Same-Day Service Available

C. **Neighborhoods section** (`bg-white py-16`):
   - H2: `Neighborhoods We Serve in {area.city}`
   - `area.neighborhoods` rendered as display-only pills (no links): `rounded-full border border-border px-4 py-2 text-sm font-semibold text-text-secondary`
   - Wrap in `<ScrollReveal>`

D. **Local Context / Description section** (`bg-off-white py-16`):
   - H2: `About Our Plumbing Services in {area.city}`
   - Render all 3 `area.localContext[]` entries as `<p>` tags with `font-body text-text-secondary text-[16px] leading-relaxed` and `space-y-6` wrapper
   - Wrap in `<ScrollReveal>`

E. **Area Facts callout box** (inside the Local Context section, after paragraphs):
   - Highlighted callout box: `bg-primary/5 border border-primary/20 rounded-lg p-6`
   - H3: `Plumbing Facts About {area.city}`
   - Render `area.areaFacts[]` as a bulleted list with check icons (green check, same pattern as commonProblems in service pages but simpler)

F. **Our Services in {City} section** (`bg-white py-16`):
   - H2: `Our Services in {area.city}`
   - 4-card grid: `grid grid-cols-1 sm:grid-cols-2 gap-6`
   - Each card links to `/services/${service.slug}` with icon, title, shortDescription, "Learn More" arrow
   - Duplicate SERVICE_ICONS map (same as services/page.tsx and services/[slug]/page.tsx — known duplication, accepted)
   - Import `services` from `@/lib/data/services`
   - Wrap in `<ScrollReveal>`

G. **We Also Serve section** (`bg-off-white py-16`):
   - H2: `We Also Serve`
   - Filter out current area: `areas.filter(a => a.slug !== area.slug)`
   - Render other 7 as pill links with map pin icon: `<Link href="/service-areas/${other.slug}">` with same pill styling as service page area pills
   - Wrap in `<ScrollReveal>`

H. **Bottom CTA strip** (`bg-dark py-14`):
   - H2: `Need a Plumber in {area.city}?`
   - Subtext about calling for free estimate
   - Dual CTA buttons (call + contact link)
   - Trust footer line (license, rating)
   - Same pattern as non-emergency service page bottom CTA
   - Wrap in `<ScrollReveal>`

**Important implementation notes:**
- NO FAQ section (no FAQ data exists for areas — CONTEXT.md says skip)
- NO emergency variant (all areas use standard teal/primary styling)
- NO pricing sidebar (areas don't have pricing data)
- Use `notFound()` from `next/navigation` if area not found
- Import `ScrollReveal` from `@/components/ScrollReveal`
- Import `JsonLd` from `@/components/JsonLd`
  </action>
  <verify>
    Run `npx tsc --noEmit` — no type errors.
    Run `npm run build` — all 8 area routes + index appear in build output.
    Verify build output shows `/service-areas/la-vista` (hyphenated slug generates correctly).
  </verify>
  <done>
    All 8 area routes pre-render at build time. Each has unique H1 ("Plumber in [City], NE"), unique localContext paragraphs, neighborhood pills, area facts callout, 4-service card grid, "We Also Serve" cross-links, and bottom CTA. Schema generator produces city-scoped Plumber JSON-LD.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create index page and update sitemap</name>
  <files>
    src/app/service-areas/page.tsx
    src/app/sitemap.ts
  </files>
  <action>
**1. Create `src/app/service-areas/page.tsx`:**

Mirror the `/services/page.tsx` index structure.

- Static metadata via `export const metadata = generatePageMetadata(...)` (NOT async — index page has no dynamic params):
  - title: `Service Areas in Omaha Metro | Heartland Plumbing Co.`
  - description: `Licensed plumber serving Omaha, Bellevue, Papillion, La Vista, Ralston, Elkhorn, Gretna & Bennington, NE. Local plumbing experts. Call (402) 555-0147.` (keep under 155 chars)
  - path: `/service-areas`

- BreadcrumbList JSON-LD: `[Home, Service Areas]` (2-level, same as /services index)

- **Banner section** (`bg-dark py-16 lg:py-20`):
  - Visual breadcrumb: Home / Service Areas
  - H1: `Plumbing Service Areas — Omaha Metro`
  - Subtext about serving 8 communities across the metro
  - Dual CTA (call + contact)

- **Area cards grid** (`bg-white py-20`):
  - `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6` (3 cols on desktop for 8 items)
  - Each card: `<Link href="/service-areas/${area.slug}">` with:
    - Map pin icon (same as area pills on service pages)
    - H2: `Plumber in {area.city}, NE`
    - Teaser: first 120 characters of `area.localContext[0]` + "..." (truncated preview)
    - "Learn More" arrow link text
    - Same card styling as services grid: `bg-white border border-border rounded-lg p-8 hover:border-primary hover:shadow-card transition-all`
  - Wrap in `<ScrollReveal>`

**2. Update `src/app/sitemap.ts`:**

Add `/service-areas` index URL to the sitemap. Insert it alongside the existing `/services` equivalent (which is not there either — but that's a separate concern).

Add this entry to the return array, before `...serviceEntries`:
```typescript
{
  url: `${BASE}/service-areas`,
  lastModified: new Date(),
  changeFrequency: 'monthly' as const,
  priority: 0.6,
},
```

Also add `/services` index if it's missing (check first — it may already be there). Same priority 0.6.
  </action>
  <verify>
    Run `npm run build` — `/service-areas` route appears in build output.
    Check build output for sitemap.xml generation with no errors.
    Verify the sitemap includes `/service-areas` as a standalone entry.
  </verify>
  <done>
    /service-areas index page renders with all 8 area cards. Sitemap includes /service-areas as a standalone URL entry. Breadcrumb path Home > Service Areas resolves without 404.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with zero errors
2. Build output lists all 9 routes: /service-areas, /service-areas/omaha, /service-areas/bellevue, /service-areas/papillion, /service-areas/la-vista, /service-areas/ralston, /service-areas/elkhorn, /service-areas/gretna, /service-areas/bennington
3. `npx tsc --noEmit` passes with no type errors
4. No duplicate H1 or title tag across any two area pages (each pulls from unique metaTitle in service-areas.ts)
</verification>

<success_criteria>
- All 8 area pages pre-render at build time via generateStaticParams
- Each area page has: unique H1, unique title/description, neighborhood pills, localContext paragraphs, areaFacts callout, 4-service grid, "We Also Serve" links, bottom CTA
- /service-areas index exists and lists all 8 areas
- City-scoped Plumber schema renders on each area page
- BreadcrumbList schema on each area page (3-level) and index (2-level)
- Sitemap includes /service-areas index URL
- npm run build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-service-area-pages/05-01-SUMMARY.md`
</output>
