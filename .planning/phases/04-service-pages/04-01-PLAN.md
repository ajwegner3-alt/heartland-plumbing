---
phase: 04-service-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/services/[slug]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Navigating to /services/drain-cleaning returns a pre-rendered page with Drain Cleaning content"
    - "Navigating to /services/water-heaters returns a pre-rendered page with Water Heaters content"
    - "Navigating to /services/sewer-line-repair returns a pre-rendered page with Sewer Line Repair content"
    - "Navigating to /services/emergency-plumbing returns a pre-rendered page with red/urgent styling and giant phone number"
    - "Each service page shows service description, common problems, and pricing range from services.ts data"
    - "The Emergency Plumbing page shows a 24/7 badge and Call Now as the primary CTA"
    - "Navigating to /services/bad-slug returns 404"
  artifacts:
    - path: "src/app/services/[slug]/page.tsx"
      provides: "Dynamic service page route with SSG"
      exports: ["generateStaticParams", "generateMetadata", "default"]
  key_links:
    - from: "src/app/services/[slug]/page.tsx"
      to: "src/lib/data/services.ts"
      via: "services.find((s) => s.slug === slug)"
      pattern: "services\\.find"
    - from: "src/app/services/[slug]/page.tsx"
      to: "src/lib/metadata.ts"
      via: "generatePageMetadata({ title: service.metaTitle })"
      pattern: "generatePageMetadata"
    - from: "src/app/services/[slug]/page.tsx"
      to: "src/lib/data/business.ts"
      via: "BUSINESS.phone, BUSINESS.phoneHref for CTAs"
      pattern: "BUSINESS\\.phone"
---

<objective>
Build the dynamic service page route at src/app/services/[slug]/page.tsx that renders all four service pages from services.ts data. Each page displays a banner, service description paragraphs, common problems list, pricing context, and CTA sections. The Emergency Plumbing page gets distinct red/urgent styling with a giant phone number and 24/7 badge.

Purpose: Fulfills SERV-01 through SERV-05 and CONV-09 — each service has a dedicated page with unique content, pricing guidance, and conversion-optimized CTAs.
Output: A working dynamic route that pre-renders 4 service pages at build time.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-service-pages/04-CONTEXT.md
@.planning/phases/04-service-pages/04-RESEARCH.md
@src/lib/data/services.ts
@src/lib/data/business.ts
@src/lib/metadata.ts
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dynamic service page route with content sections and emergency styling</name>
  <files>src/app/services/[slug]/page.tsx</files>
  <action>
Create `src/app/services/[slug]/page.tsx` as an async Server Component. This single file handles all 4 services.

**Route setup:**
- Export `generateStaticParams()` returning `services.map((s) => ({ slug: s.slug }))` for SSG pre-rendering
- Export `async function generateMetadata({ params })` — await params (Next.js 16 Promise pattern), find service by slug, return `generatePageMetadata({ title: service.metaTitle, description: service.metaDescription, path: '/services/${service.slug}' })`
- Export default async function `ServicePage({ params })` — await params, find service, call `notFound()` if not found

**Emergency detection:**
- `const isEmergency = service.slug === 'emergency-plumbing'`
- Use conditional classes throughout for emergency vs standard styling

**Page sections (standard service pages):**

1. **Page Banner** — dark background (`bg-dark`), full-width. Contains:
   - Breadcrumb nav: Home > Services > [Service Title] (text links, `text-white/60` with separators)
   - H1: `service.title` in white, `font-display font-black text-4xl lg:text-5xl`
   - Short description: `service.shortDescription` in `text-white/70`
   - Dual CTA: "Call {BUSINESS.phone}" button (bg-primary) + "Get Free Estimate" link to /contact (border button)
   - Trust micro-bar below CTAs: "Licensed & Insured · PL-{BUSINESS.license} · 4.9★ 312 Reviews" in `text-white/60 text-sm`
   - Container: `max-w-[1320px] mx-auto px-[36px]`, section padding `py-16 lg:py-20`

2. **Common Problems Section** — white bg, `py-16`. Contains:
   - H2: "Common Signs You Need {service.title}" (or similar natural phrasing)
   - Grid of `service.commonProblems` items, each with a green checkmark/alert icon and the problem text
   - Layout: `grid grid-cols-1 sm:grid-cols-2 gap-4`

3. **Service Description Section** — off-white bg, `py-16`. Contains:
   - H2: "About Our {service.title} Services" (or natural variant)
   - Map `service.description[]` paragraphs as `<p>` elements with `font-body text-text-secondary text-[16px] leading-relaxed mb-6`

4. **Pricing Context** — inline within description section or its own small subsection:
   - A styled callout or paragraph: "Typical pricing: {service.pricingRange}" — NOT a separate pricing table, just inline per CONTEXT.md decision

5. **Bottom CTA Strip** — dark bg, `py-12`. Phone number link + "Get Free Estimate" link. Same dual-CTA pattern as banner.

**Emergency page overrides:**
- Banner bg changes to `bg-red-900` (or `bg-[#7f1d1d]`)
- Add a "24/7 Emergency Service" badge at the top of the banner — red bg pill with white text, before the breadcrumb
- Giant phone number: `<a href={BUSINESS.phoneHref}> className="font-display font-black text-4xl lg:text-5xl text-white">{BUSINESS.phone}</a>` — prominent, immediately visible
- CTA buttons use `bg-red-600 hover:bg-red-700` instead of `bg-primary hover:bg-primary-dark`
- Bottom CTA strip also uses red accent

**Styling conventions (match homepage):**
- Container: `max-w-[1320px] mx-auto px-[36px]`
- Font tokens: `font-display` for headings, `font-body` for body text
- Color tokens: `text-primary`, `bg-off-white`, `border-border`, `text-text-primary`, `text-text-secondary`
- Wrap below-fold sections in `<ScrollReveal>` for fade-in animation

**Do NOT include in this task (deferred to Plan 04-02):**
- FAQ accordion section
- Schema JSON-LD injection (Service, FAQPage, Breadcrumb)
- Related services cross-link cards
- Service areas pill links
- /services index page

**Imports needed:**
```typescript
import { notFound } from 'next/navigation'
import Link from 'next/link'
import { ScrollReveal } from '@/components/ScrollReveal'
import { generatePageMetadata } from '@/lib/metadata'
import { services } from '@/lib/data/services'
import { BUSINESS } from '@/lib/data/business'
```
  </action>
  <verify>
Run `npm run build` — build succeeds with all 4 service pages pre-rendered (check build output for `/services/drain-cleaning`, `/services/water-heaters`, `/services/sewer-line-repair`, `/services/emergency-plumbing`).

Run `npm run dev` and visit each URL manually — all 4 pages render content from services.ts data, emergency page has red styling and giant phone number.
  </verify>
  <done>
All 4 service URLs return pre-rendered pages with unique content. Emergency page has red banner, 24/7 badge, and giant tappable phone number. Standard pages show teal primary CTA. Each page displays description paragraphs, common problems, and pricing range. Invalid slugs return 404.
  </done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors and shows all 4 service routes in build output
- Each service page H1 matches the service title from services.ts
- Emergency page visually distinct with red accent and prominent phone number
- generateMetadata produces unique title/description per page (inspect page source or build output)
</verification>

<success_criteria>
- 4 service pages pre-rendered at build time via generateStaticParams
- Each page content driven by services.ts data (not hardcoded)
- Emergency page has red/urgent treatment with giant phone CTA and 24/7 badge
- Standard pages have teal primary CTA
- Invalid slug returns 404 via notFound()
- All design tokens match homepage conventions
</success_criteria>

<output>
After completion, create `.planning/phases/04-service-pages/04-01-SUMMARY.md`
</output>
