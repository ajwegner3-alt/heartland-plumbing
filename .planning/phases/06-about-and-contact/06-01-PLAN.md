---
phase: 06-about-and-contact
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/data/business.ts
  - src/app/about/page.tsx
  - src/lib/email/lead-notification.ts
autonomous: true

must_haves:
  truths:
    - "The About page displays the company story with 2-3 paragraphs covering founding in 1998, mission, and what sets Heartland apart"
    - "An owner spotlight card shows a name, title, and brief personal bio"
    - "Credentials appear as visual badge cards: Licensed PL-28541, Insured $2M, BBB A+, 25+ years experience"
    - "BUSINESS.email resolves to info@heartlandplumbingomaha.com for use by the contact route handler"
  artifacts:
    - path: "src/app/about/page.tsx"
      provides: "About page with story, owner card, credentials"
      min_lines: 80
    - path: "src/lib/email/lead-notification.ts"
      provides: "Branded HTML email template function"
      exports: ["buildLeadEmail"]
    - path: "src/lib/data/business.ts"
      provides: "BUSINESS constant with email field added"
      contains: "email:"
  key_links:
    - from: "src/app/about/page.tsx"
      to: "src/lib/data/business.ts"
      via: "import BUSINESS"
      pattern: "import.*BUSINESS.*from.*business"
    - from: "src/app/about/page.tsx"
      to: "src/lib/metadata.ts"
      via: "generatePageMetadata"
      pattern: "generatePageMetadata"
---

<objective>
Build the About page and supporting data/email infrastructure for Phase 06.

Purpose: The About page establishes trust and credibility for visitors. The email template and BUSINESS.email field are required by Plan 02's contact form route handler.
Output: A live /about route with company story, owner spotlight, and credentials badges. A reusable buildLeadEmail() function. BUSINESS.email field added.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-about-and-contact/06-CONTEXT.md
@.planning/phases/06-about-and-contact/06-RESEARCH.md

@src/lib/data/business.ts
@src/lib/metadata.ts
@src/app/page.tsx
@src/app/globals.css
@src/components/JsonLd.tsx
@src/components/ScrollReveal.tsx
@src/lib/schema/breadcrumb.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add email field to BUSINESS and build email template</name>
  <files>
    src/lib/data/business.ts
    src/lib/email/lead-notification.ts
  </files>
  <action>
    1. In `src/lib/data/business.ts`:
       - Add `email: string` to the `BusinessData` interface
       - Add `email: 'info@heartlandplumbingomaha.com'` to the BUSINESS constant

    2. Create `src/lib/email/lead-notification.ts`:
       - Export a `buildLeadEmail(data: LeadData): string` function
       - Interface: `{ name: string; phone: string; service?: string; zip?: string; message?: string }`
       - Returns an HTML string with inline styles (email clients don't support Tailwind)
       - Use the teal brand color `#1a7a6e` for the header bar
       - Table layout: Name, Phone (as tel: link), Service, Zip, Message
       - Include an `escapeHtml()` helper that replaces `<`, `>`, `&`, `"` with HTML entities
       - Apply escapeHtml to ALL user-supplied fields before interpolation (XSS prevention)
       - Match the exact pattern from 06-RESEARCH.md Pattern 3, but add escapeHtml wrapping
  </action>
  <verify>
    `npx tsc --noEmit` passes with no errors on business.ts and lead-notification.ts
  </verify>
  <done>
    BUSINESS.email equals 'info@heartlandplumbingomaha.com'. buildLeadEmail returns an HTML string with escaped user input and branded styling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build the About page</name>
  <files>
    src/app/about/page.tsx
  </files>
  <action>
    Build `src/app/about/page.tsx` as a Server Component (no 'use client'). Follow existing page patterns exactly:

    **Metadata:**
    - Use `generatePageMetadata()` with title "About Us | Heartland Plumbing Co." (or similar under 60 chars), description 150-155 chars about the company story + CTA, path "/about"

    **Schema:**
    - Include BreadcrumbList schema (Home > About) using the existing breadcrumb generator
    - Include LocalBusiness schema (reuse existing generator from Phase 2)

    **Page sections (all wrapped in ScrollReveal where appropriate):**

    1. **Hero/Banner** — Teal background banner with H1 "About Heartland Plumbing Co." and a brief subtitle. Match the banner pattern from service/area pages (dark teal bg, white text, breadcrumb).

    2. **Company Story** — 2-3 paragraphs covering:
       - Founded in 1998 in Omaha (compute years dynamically: `new Date().getFullYear() - BUSINESS.yearsFounded`)
       - Mission: reliable, honest plumbing for homeowners and businesses
       - What sets them apart: local roots, fair pricing, clean work
       - Use realistic contractor-appropriate copy. No corporate buzzwords.

    3. **Owner Spotlight Card** — A card with:
       - Name: "Mike Richardson" (fictional owner)
       - Title: "Owner & Master Plumber"
       - Brief 2-3 sentence bio about hands-on leadership
       - Use a placeholder icon/initial avatar (no external image)
       - Card styling: white bg, rounded, subtle shadow, border-border

    4. **Credentials Badge Cards** — A grid of 4 badge-style cards:
       - Licensed Plumber: PL-28541 (from BUSINESS.license)
       - Fully Insured: $2M liability coverage
       - BBB Accredited: A+ Rating
       - 25+ Years Experience (compute dynamically)
       - Each card: icon (use simple SVG or emoji), title, detail text
       - Grid: 2 cols on mobile, 4 on desktop

    5. **Bottom CTA** — "Ready to Work With Us?" section with phone link and link to /contact page

    **Styling rules:**
    - Use existing design tokens: font-display for headings, font-body for text
    - Colors: text-text-primary, text-text-muted, bg-surface, bg-dark, text-primary (teal)
    - Match spacing/padding patterns from existing pages (container mx-auto, section py-16/20)
    - No client JS. Pure server component.
  </action>
  <verify>
    `npm run build` succeeds. Navigate to /about in dev — page renders with all 5 sections visible, no console errors.
  </verify>
  <done>
    /about renders a complete About page with company story (2-3 paragraphs), owner spotlight card (Mike Richardson), 4 credential badge cards (license, insurance, BBB, years), and a bottom CTA linking to /contact. BreadcrumbList and LocalBusiness schemas are present in page source.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — no type errors
- `npm run build` — builds successfully, /about route appears in output
- BUSINESS object has `email` field
- `buildLeadEmail` function exists and exports correctly
- /about page has proper metadata, schema, and all content sections
</verification>

<success_criteria>
1. /about route renders with company story, owner card, and credential badges
2. BUSINESS.email is available for import by the contact route handler
3. buildLeadEmail() is ready for use by the contact route handler
4. Page follows existing design patterns (fonts, colors, spacing, ScrollReveal)
</success_criteria>

<output>
After completion, create `.planning/phases/06-about-and-contact/06-01-SUMMARY.md`
</output>
