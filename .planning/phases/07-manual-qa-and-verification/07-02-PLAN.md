---
phase: 07-manual-qa-and-verification
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - FUTURE_DIRECTIONS.md
  - .planning/phases/07-manual-qa-and-verification/qa-checklist.md
autonomous: false

must_haves:
  truths:
    - "All 16 pages render correctly in the browser with no console errors"
    - "Schema markup passes Google Rich Results Test for all types"
    - "Contact form displays success state when submitted with valid data"
    - "Sitemap.xml is accessible at the deployed URL and lists all pages"
    - "FUTURE_DIRECTIONS.md exists in the project root"
  artifacts:
    - path: ".planning/phases/07-manual-qa-and-verification/qa-checklist.md"
      provides: "Manual QA results for all pages"
      min_lines: 40
    - path: "FUTURE_DIRECTIONS.md"
      provides: "Known limitations, future improvements, technical debt"
      min_lines: 30
  key_links:
    - from: "qa-checklist.md"
      to: "All 16 deployed pages"
      via: "Manual browser inspection"
      pattern: "PASS|FAIL"
---

<objective>
Complete manual QA verification of all pages, validate schema markup, test the contact form, and generate the FUTURE_DIRECTIONS.md project completion report.

Purpose: This is the final gate before the project is marked complete. Andrew must visually verify the deployed site, and the project requires a FUTURE_DIRECTIONS.md per global CLAUDE.md instructions.

Output: qa-checklist.md with pass/fail results, FUTURE_DIRECTIONS.md in project root.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-manual-qa-and-verification/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated QA checks and generate QA checklist for manual verification</name>
  <files>.planning/phases/07-manual-qa-and-verification/qa-checklist.md</files>
  <action>
    Part A — Automated checks Claude can run:

    1. Build verification: Run `npm run build` and confirm all 16 pages are generated (check the build output for route count).

    2. Sitemap verification: Fetch `{VERCEL_URL}/sitemap.xml` using curl or web_fetch. Confirm it contains all expected URLs:
       - / (homepage)
       - /about
       - /contact
       - /services (index)
       - /services/drain-cleaning, /services/water-heaters, /services/sewer-line-repair, /services/emergency-plumbing
       - /service-areas (index)
       - /service-areas/omaha, /service-areas/bellevue, /service-areas/papillion, /service-areas/la-vista, /service-areas/ralston, /service-areas/elkhorn, /service-areas/gretna, /service-areas/bennington
       Count should be 16 URLs total.

    3. Schema validation: For each page type, fetch the HTML and grep for JSON-LD script tags. Verify:
       - Homepage: LocalBusiness/Plumber schema + AggregateRating
       - Service pages: Service + FAQPage + BreadcrumbList schemas
       - Area pages: BreadcrumbList schema
       - About page: BreadcrumbList schema
       - Contact page: BreadcrumbList schema

    4. Console error check: Use Lighthouse's --output json (from Plan 01 results) to check for any console errors reported. Or run `npx lighthouse {VERCEL_URL} --only-categories=best-practices --output json` and parse for console error audits.

    5. robots.txt: Fetch `{VERCEL_URL}/robots.txt` and confirm it allows crawling of all important pages.

    Part B — Generate the manual QA checklist:

    Create `.planning/phases/07-manual-qa-and-verification/qa-checklist.md` with this structure:

    ```markdown
    # Manual QA Checklist — Heartland Plumbing Co.

    **Deployed URL:** {VERCEL_URL}
    **Date:** {today}
    **Tester:** Andrew

    ## Instructions
    Open each URL in Chrome. Check each item. Mark PASS or FAIL.

    ## Global Checks
    - [ ] Header is sticky and visible on scroll (all pages)
    - [ ] Phone number in header is tappable (tel: link works on mobile)
    - [ ] Mobile hamburger menu opens/closes smoothly
    - [ ] Services dropdown works on hover (desktop) and tap (mobile)
    - [ ] Sticky mobile CTA bar visible on mobile, hidden on desktop
    - [ ] Footer renders consistently on all pages
    - [ ] No browser console errors on any page (open DevTools → Console)

    ## Page-by-Page Checks

    ### Homepage (/)
    - [ ] Hero loads with teal/copper branding, CTA visible above fold
    - [ ] Trust stats bar shows 25+ years, 312 reviews, 4.9 stars
    - [ ] Services grid shows 4 services with working links
    - [ ] Testimonials section displays 3+ reviews with names and cities
    - [ ] Service area section links to all 8 area pages

    ### About (/about)
    - [ ] Company story section renders
    - [ ] License number PL-28541 is visible
    - [ ] Owner spotlight card displays
    - [ ] Breadcrumb navigation works

    ### Contact (/contact)
    - [ ] Form displays with name, phone, service, zip fields
    - [ ] Phone field is prominent/first
    - [ ] Submit with empty fields shows validation errors
    - [ ] Submit with valid data shows success message (email delivery not expected — portfolio demo)
    - [ ] Breadcrumb navigation works

    ### Services Index (/services)
    - [ ] Lists all 4 services with links

    ### Service Pages (check at least 2)
    - [ ] /services/drain-cleaning — content loads, FAQ accordion works, cross-links present
    - [ ] /services/emergency-plumbing — red/urgent styling visible, 24/7 messaging, "Call Now" CTA

    ### Service Area Pages (check at least 2)
    - [ ] /service-areas/omaha — unique local content, service links, breadcrumb
    - [ ] /service-areas/bellevue — different content from Omaha page

    ## Schema Validation
    - [ ] Go to https://search.google.com/test/rich-results
    - [ ] Test homepage URL — LocalBusiness detected
    - [ ] Test a service page URL — Service + FAQ detected
    - [ ] Test an area page URL — BreadcrumbList detected

    ## Sitemap
    - [ ] Visit {VERCEL_URL}/sitemap.xml — all 16 URLs listed
    - [ ] Visit {VERCEL_URL}/robots.txt — allows crawling

    ## Final Sign-off
    - [ ] All checks PASS
    - [ ] Lighthouse 95+ confirmed (from Plan 07-01)
    - [ ] Project approved for completion
    ```

    Fill in the automated check results (sitemap count, schema presence, robots.txt) directly. Leave manual visual checks as empty checkboxes for Andrew.
  </action>
  <verify>
    - qa-checklist.md exists with all sections populated
    - Automated checks (sitemap, schema, robots.txt) have results filled in
    - Manual checks are left as empty checkboxes
  </verify>
  <done>QA checklist generated with automated results filled in and manual verification items ready for Andrew's review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete QA checklist with automated results pre-filled. The checklist covers all 16 pages, schema validation, sitemap, and form testing. Lighthouse scores from Plan 07-01 are already confirmed.
  </what-built>
  <how-to-verify>
    1. Open the deployed Vercel URL in Chrome on both desktop and mobile (or Chrome DevTools responsive mode)
    2. Work through the checklist in `.planning/phases/07-manual-qa-and-verification/qa-checklist.md`
    3. For each item, verify it visually and mark PASS or FAIL
    4. Open Chrome DevTools Console on each page to check for errors
    5. Test the contact form: submit with empty fields (should show errors), then submit with valid data (should show success)
    6. Visit https://search.google.com/test/rich-results and test 2-3 URLs for schema validation
    7. Report any FAIL items so they can be fixed before project completion
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or list any FAIL items that need fixing</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Generate FUTURE_DIRECTIONS.md project completion report</name>
  <files>FUTURE_DIRECTIONS.md</files>
  <action>
    Create FUTURE_DIRECTIONS.md in the project root with these sections (required by global CLAUDE.md):

    **1. Known Limitations**
    - Contact form email delivery not configured (Resend API key not added — portfolio demo only)
    - No real Google Business Profile connected
    - No analytics/tracking installed (GA4, Search Console)
    - No call tracking integration
    - Review data is static/hardcoded, not pulled from Google
    - No blog or content marketing section
    - Map on contact page is a placeholder, not a live Google Maps embed
    - Images are placeholder/generated, not real job site photos

    **2. Assumptions and Constraints**
    - Built as a portfolio demo for NSI, not a live client site
    - Static data (services, areas, reviews) — no CMS or database for content editing
    - Resend chosen for email but requires domain verification for production use
    - 8 service areas chosen as representative Omaha metro communities
    - 4 core plumbing services chosen — a real client would likely have more
    - SEO titles/descriptions optimized for Omaha market specifically

    **3. Future Improvements**
    - Add GA4 and Search Console integration for traffic tracking
    - Connect Google Business Profile API for live review data
    - Add before/after project gallery with real job photos
    - Implement online scheduling/booking (Calendly or custom)
    - Add blog section for content marketing and SEO authority
    - Create Google Sheets CRM integration for form submissions via n8n
    - Add seasonal promotion banner capability
    - Expand to 10-15 service pages for broader keyword coverage
    - Add more service areas as client's territory grows
    - Implement call tracking (CallRail or similar) for conversion attribution

    **4. Technical Debt**
    - SERVICE_ICONS map duplicated between service pages and area pages (should extract to shared module)
    - Tailwind v4 + @tailwindcss/typography compatibility unconfirmed (not needed currently but would be for blog)
    - Owner avatar is a CSS circle with initial letter — should be replaced with real photo
    - Form honeypot field name could be more deceptive (currently generic)
    - No automated testing (unit or E2E) — acceptable for portfolio demo but needed for production

    Commit the file: `git add FUTURE_DIRECTIONS.md && git commit -m "docs: add FUTURE_DIRECTIONS.md project completion report"`
  </action>
  <verify>
    - FUTURE_DIRECTIONS.md exists in project root
    - Contains all 4 required sections
    - `git log --oneline -1` shows the commit
  </verify>
  <done>FUTURE_DIRECTIONS.md committed to repo with Known Limitations, Assumptions, Future Improvements, and Technical Debt sections.</done>
</task>

</tasks>

<verification>
- qa-checklist.md exists with automated results and Andrew's manual sign-off
- FUTURE_DIRECTIONS.md exists in project root and is committed
- All 16 pages verified with no console errors
- Schema markup validated via Google Rich Results Test
- Contact form tested (UI validation works; email delivery skipped for portfolio demo)
- Sitemap.xml accessible and contains 16 URLs
</verification>

<success_criteria>
Andrew has manually verified all pages on the deployed URL and approved the project. FUTURE_DIRECTIONS.md is committed. The project is ready to be marked complete.
</success_criteria>

<output>
After completion, create `.planning/phases/07-manual-qa-and-verification/07-02-SUMMARY.md`
</output>
