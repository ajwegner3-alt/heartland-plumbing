---
phase: 07-manual-qa-and-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/about/page.tsx
  - src/app/contact/page.tsx
  - src/app/services/[slug]/page.tsx
  - src/app/service-areas/[slug]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Lighthouse Performance score is 95+ on the deployed homepage"
    - "Lighthouse Accessibility score is 95+ on the deployed homepage"
    - "Lighthouse Best Practices score is 95+ on the deployed homepage"
    - "Lighthouse SEO score is 95+ on the deployed homepage"
    - "No console errors on any page"
  artifacts:
    - path: ".planning/phases/07-manual-qa-and-verification/lighthouse-results.md"
      provides: "Documented Lighthouse scores for all tested pages"
      min_lines: 20
  key_links:
    - from: "Lighthouse CLI"
      to: "Deployed Vercel URL"
      via: "npx lighthouse --output json"
      pattern: "lighthouse"
---

<objective>
Run Lighthouse audits against the deployed Vercel URL, document scores, and fix any issues that prevent 95+ across all four categories (Performance, Accessibility, Best Practices, SEO).

Purpose: PERF-06 through PERF-10 require confirmed Lighthouse scores on the LIVE deployment, not localhost. This plan automates the audit, identifies failures, fixes them, pushes fixes, and re-audits until 95+ is confirmed.

Output: lighthouse-results.md with confirmed scores, and any code fixes committed and deployed.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Get Vercel deployment URL and run Lighthouse audit on key pages</name>
  <files>.planning/phases/07-manual-qa-and-verification/lighthouse-results.md</files>
  <action>
    Step 1: Get the deployed Vercel URL.
    - Check if `vercel` CLI is available. If not, install it globally: `npm i -g vercel`
    - Run `vercel ls` or check the Vercel dashboard URL. The project is `ajwegner3-alt/heartland-plumbing`.
    - If CLI auth fails, ask Andrew for the Vercel URL. It will be something like `heartland-plumbing-xxx.vercel.app` or a custom domain.
    - IMPORTANT: If the Vercel URL is not obtainable via CLI, use the pattern `heartland-plumbing.vercel.app` and ask Andrew to confirm. Do NOT skip this — Lighthouse must run against the deployed URL, not localhost.

    Step 2: Install Lighthouse CLI if not available.
    - `npm i -g lighthouse` (or use `npx lighthouse`)

    Step 3: Run Lighthouse against the deployed homepage.
    - Command: `npx lighthouse {VERCEL_URL} --output json --output html --output-path ./lighthouse-home --chrome-flags="--headless --no-sandbox"`
    - Parse the JSON output to extract scores for: Performance, Accessibility, Best Practices, SEO
    - Record LCP, CLS, and INP values from the Performance diagnostics

    Step 4: Run Lighthouse against 2-3 additional representative pages:
    - A service page: `{VERCEL_URL}/services/drain-cleaning`
    - An area page: `{VERCEL_URL}/service-areas/omaha`
    - The contact page: `{VERCEL_URL}/contact`

    Step 5: Document ALL scores in `.planning/phases/07-manual-qa-and-verification/lighthouse-results.md` with this format:
    ```
    # Lighthouse Audit Results
    ## Run 1 — [date]
    | Page | Perf | A11y | Best Practices | SEO | LCP | CLS | INP |
    |------|------|------|----------------|-----|-----|-----|-----|
    | Homepage | XX | XX | XX | XX | X.Xs | X.XX | XXms |
    | /services/drain-cleaning | ... | ... | ... | ... | ... | ... | ... |
    | /service-areas/omaha | ... | ... | ... | ... | ... | ... | ... |
    | /contact | ... | ... | ... | ... | ... | ... | ... |

    ### Issues Found
    - [list each audit that scored below 95]
    - [include the specific failing audit name and recommendation]
    ```

    Step 6: If ANY score is below 95, identify the specific failing audits from the JSON output. Common fixes for Next.js SSG sites:
    - Accessibility: missing aria-labels on icon buttons, missing alt text, color contrast, heading hierarchy
    - Best Practices: console errors, deprecated APIs, missing HTTPS redirects
    - SEO: missing meta descriptions (already handled), robots blocking
    - Performance: unoptimized images (already using next/image), render-blocking resources, unused CSS

    Apply fixes to the relevant source files. Keep fixes minimal and targeted — only address what Lighthouse flagged.
  </action>
  <verify>
    - lighthouse-results.md exists with scores for 4 pages
    - `npm run build` succeeds after any fixes
  </verify>
  <done>Lighthouse scores documented for homepage + 3 representative pages. Any failing audits identified with specific fix recommendations.</done>
</task>

<task type="auto">
  <name>Task 2: Fix failing Lighthouse audits and re-run until 95+ confirmed</name>
  <files>
    src/app/layout.tsx
    src/app/page.tsx
    src/app/about/page.tsx
    src/app/contact/page.tsx
    src/app/services/[slug]/page.tsx
    src/app/service-areas/[slug]/page.tsx
    .planning/phases/07-manual-qa-and-verification/lighthouse-results.md
  </files>
  <action>
    Based on the failing audits identified in Task 1, apply targeted fixes. Common patterns:

    **Accessibility fixes (most likely needed):**
    - Add aria-label to the mobile hamburger button if missing
    - Add aria-label to social media icon links in footer
    - Ensure all interactive elements have accessible names
    - Check color contrast ratios on teal/copper brand colors against backgrounds
    - Verify form labels are properly associated with inputs (ContactForm)

    **Best Practices fixes:**
    - Remove any console.log statements left in production code
    - Ensure all links with target="_blank" have rel="noopener noreferrer"
    - Check for any deprecated API usage

    **SEO fixes:**
    - Verify meta robots is not accidentally set to noindex on important pages
    - Confirm all pages have unique title and description (already built into generatePageMetadata)

    **Performance fixes (if needed):**
    - Ensure no layout shift from fonts (next/font handles this)
    - Check that hero image has priority={true} and explicit width/height
    - Verify no unnecessary client-side JavaScript is loading

    After applying fixes:
    1. Run `npm run build` to confirm no build errors
    2. Push to master: `git add -A && git commit -m "fix(07): address Lighthouse audit failures" && git push origin master`
    3. Wait for Vercel deployment (check `vercel ls` or wait ~60 seconds)
    4. Re-run Lighthouse on the homepage: `npx lighthouse {VERCEL_URL} --output json --chrome-flags="--headless --no-sandbox"`
    5. Update lighthouse-results.md with "Run 2" scores
    6. If still below 95 on any category, repeat the fix cycle (up to 3 iterations max)

    If 95+ is confirmed on all 4 categories:
    - Update lighthouse-results.md with a "CONFIRMED" status line
    - Record the exact scores as the final baseline

    If 95+ cannot be achieved after 3 fix cycles:
    - Document which category is failing and why
    - Note if the issue is environmental (Vercel cold start, CDN caching) vs code-related
    - Flag for Andrew's review
  </action>
  <verify>
    - `npm run build` succeeds
    - lighthouse-results.md shows "Run 2" (or later) with all scores 95+, or documents why a score cannot be achieved
    - All fixes committed and pushed to master
  </verify>
  <done>Lighthouse 95+ confirmed on all four categories for the deployed homepage, or documented exceptions with reasoning. Fixes committed and deployed.</done>
</task>

</tasks>

<verification>
- lighthouse-results.md exists with tabular scores for 4+ pages
- All four Lighthouse categories show 95+ on the deployed homepage
- LCP < 2.5s, CLS < 0.1, INP < 200ms documented
- Any code fixes are committed with descriptive messages
- Build passes after all changes
</verification>

<success_criteria>
PERF-06, PERF-07, PERF-08, PERF-09 confirmed with documented evidence. PERF-10 (Core Web Vitals) values recorded. All fixes deployed to Vercel.
</success_criteria>

<output>
After completion, create `.planning/phases/07-manual-qa-and-verification/07-01-SUMMARY.md`
</output>
